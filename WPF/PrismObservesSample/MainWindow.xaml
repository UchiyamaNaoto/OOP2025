<Window x:Class="PrismObservesSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismObservesSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 入力１ -->
        <TextBox Grid.Row="0" Grid.Column="0" Height="30" Margin="5"
                 Text="{Binding Input1,UpdateSourceTrigger=PropertyChanged}"/>
        <!-- ＋記号 -->
        <TextBlock Grid.Row="0" Grid.Column="1" Height="30" Margin="5"
                   Text="+" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <!-- 入力２ -->
        <TextBox Grid.Row="0" Grid.Column="2" Height="30" Margin="5"
                 Text="{Binding Input2,UpdateSourceTrigger=PropertyChanged}"/>
        <!-- ＝記号 -->
        <TextBlock Grid.Row="0" Grid.Column="3" Height="30" Margin="5"
           Text="=" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <!-- 計算結果 -->
        <TextBlock Grid.Row="0" Grid.Column="4" Height="30" Margin="5"
                   Text="{Binding Result}"/>
        <!-- 計算ボタン -->
        <Button Grid.Row="1" Grid.Column="4" Margin="4" Content="計算" 
                Command="{Binding SumCommand}"/>
    </Grid>
</Window>
