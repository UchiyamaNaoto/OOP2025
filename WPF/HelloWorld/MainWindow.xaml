<Window x:Class="HelloWorld.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWorld"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="200">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding GreetingMessage}"/>
        <Button Content="ChangeMessage" Grid.Row="1" 
                Command="{Binding ChangeMessageCommand}"
                CommandParameter="{Binding NewMessage1}"
                IsEnabled="{Binding CanChangeMessage}"/> <!--　←削除から-->

        <Button Content="ChangeMessage" Grid.Row="2"
                Command="{Binding ChangeMessageCommand}"
                CommandParameter="{Binding NewMessage2}"/>
    </Grid>
</Window>
